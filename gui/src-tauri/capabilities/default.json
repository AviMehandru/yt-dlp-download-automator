{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window and event permissions. Everything this app can do to the filesystem or to a process goes through its own #[tauri::command] functions in main.rs, which is why no filesystem, shell, or dialog plugin permission is granted here -- there is no general-purpose 'read this path' or 'run this command' surface exposed to the frontend at all.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
