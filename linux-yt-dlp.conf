# CONFIG_VERSION: 17
# NOTE: replace /home/linuxisthebest/yt-dlp everywhere in this file (and in
# run_ytdlp.ps1, postprocess.ps1, ytdl) with your actual home directory --
# yt-dlp config files don't expand ~ the same reliable way in every context
# used here (specifically inside --exec's embedded shell command below), so
# this uses a literal placeholder rather than something that could silently
# fail to expand in just one of the several places a path appears. Fastest
# way to fix all four files in one go, from the folder containing them:
#   sed -i "s|/home/linuxisthebest/yt-dlp|$HOME/yt-dlp|g" yt-dlp.conf run_ytdlp.ps1 postprocess.ps1 ytdl
--update
-f bv*+ba/b
--merge-output-format mkv
--keep-video
--write-subs
--write-auto-subs
--sub-langs "en.*"
--embed-subs
--write-thumbnail
--convert-thumbnails png
--embed-thumbnail
--add-metadata
--embed-metadata
--write-description
--write-info-json
# No --embed-info-json here on purpose: it would embed a comment-less
# info.json (since comments haven't been fetched yet at this point in the
# pipeline). postprocess.ps1 embeds the comment-complete version itself,
# with ffmpeg directly, after the separate comments pass merges into the
# sidecar info.json below.
--no-write-playlist-metafiles
--embed-chapters
--write-link
# No --write-comments here on purpose: comments are fetched by postprocess.ps1
# in a SEPARATE pass, after this download finishes. Comment fetches on
# popular videos can take 30-60+ minutes, and the video/audio/subtitle
# URLs extracted here are signed with a short expiry -- keeping comments
# out of this pass means those links get used immediately, long before
# they'd have any chance to expire. See yt-dlp/yt-dlp#11849.
--continue
--no-overwrites
--retries infinite
--fragment-retries infinite
--extractor-retries 100
--retry-sleep "extractor:exp=1:30:2"
# Without this, a single subtitle 404 (or similar sub-step failure) aborts
# the ENTIRE video -- including throwing away comments that may have taken
# 30-60+ minutes to fetch beforehand. This downgrades that class of failure
# to a warning (logged, and now also surfaced into video_postprocessing.log)
# so the video, audio, and everything else that *did* succeed is still kept.
--ignore-errors
--sleep-requests 2
--sleep-subtitles 30
--sleep-interval 2
--max-sleep-interval 60
--concurrent-fragments 1
--download-archive "/home/linuxisthebest/yt-dlp/Archive Logs/Logs/archive.txt"
# --windows-filenames REMOVED for this Ubuntu port (it was present in the
# Windows version). Linux only forbids "/" and the null byte in filenames --
# no reserved device names, no restricted punctuation -- so this constraint
# just isn't needed here. Re-add it only if you ever need these files to
# also be safely readable from a Windows machine (dual-boot, SMB share to a
# Windows PC, etc); it's harmless to keep even on Linux if so.

# --write-playlist-metafiles is deliberately OFF: it has no sensible
# destination in a per-video path scheme (playlist-level files don't have
# a video id/date/title to fill the folder template with). Playlist/channel
# context is still captured per-video in each manifest.json and urls.json,
# and channel-level assets are handled separately by postprocess.ps1.

# --- Folder structure ---
# IMPORTANT: --paths (-P) only ever accepts a literal, unchanging PATH -- it
# does NOT expand %(uploader).40s / %(title).60s / etc, even though it looks like
# it should. Only -o/--output expands those fields (yt-dlp's own docs call
# -o's argument a "TEMPLATE" and -P's argument a plain "PATH" -- that's the
# whole distinction). So "home" below is just a static root, and every bit
# of per-video foldering (uploader/date/id/title, and which subfolder each
# file type goes in) is done entirely inside -o templates instead.
--paths "home:/home/linuxisthebest/yt-dlp/Youtube Videos/Complete Archive"
# temp can't be given a per-video subfolder for the same reason (it's a
# --paths value too), so pre-merge intermediate downloads share one flat
# scratch folder across all videos instead of getting sorted per-video.
# That's fine since they're transient and filenames still won't collide
# (the -o filename template still applies inside temp). Note --keep-video's
# retained pre-merge streams have no dedicated type to redirect either, so
# they end up alongside the merged file in "Final files" rather than a
# separate folder.
--paths "temp:/home/linuxisthebest/yt-dlp/Youtube Videos/_incomplete"
# .40s / .60s below truncate uploader/title wherever they appear in FOLDER
# names. This mattered a lot more on Windows (260-char MAX_PATH); ext4 (the
# typical Ubuntu filesystem) allows ~4096-byte total paths and 255-BYTE
# individual filenames, so this is now a much looser safety margin than a
# strict necessity -- kept anyway since there's no real downside, and
# --trim-filenames below is still a reasonable backstop regardless of OS.
# The filenames themselves are short, fixed, role-based names (Final
# Video.mkv, Subtitles.en.vtt, etc) rather than repeating the full
# uploader/date/id/title a second time -- the folder already carries that.
# yt-dlp still inserts subtitle language codes and --keep-video's format-id
# suffix automatically before the extension regardless of what base name is
# used here, so "Subtitles.%(ext)s" still becomes "Subtitles.en.vtt",
# "Subtitles.en-orig.vtt", etc, and the kept pre-merge streams still land as
# e.g. "Final Video.f137.mp4" alongside "Final Video.mkv" rather than
# colliding with it.
# (No --trim-filenames: filenames are short, fixed role-based names now --
# Final Video, Subtitles, Description, etc, all under 15 characters -- so
# there's nothing left for a length cap to meaningfully do. This existed
# before that change, when filenames still repeated the full
# uploader/date/id/title a second time, and got missed when that was
# removed. Folder-name length is still bounded separately by the .40s/.60s
# field truncation just above.
-o "%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Final files/Final Video.%(ext)s"
-o "subtitle:%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Subtitles/Subtitles.%(ext)s"
-o "thumbnail:%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Images/Thumbnail.%(ext)s"
# (No "storyboard" template: there is no --write-storyboard option in yt-dlp.
# Storyboards are only obtainable by selecting their sb0/sb1/... formats
# explicitly with -f, which doesn't fit this archival setup, so they're
# skipped rather than half-supported.)
-o "description:%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Video metadata/Description.%(ext)s"
-o "infojson:%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Video metadata/Info.%(ext)s"
-o "link:%(uploader).40s/%(uploader).40s - %(upload_date)s - %(id)s - %(title).60s/Final files/Link.%(ext)s"
# Any future write-flag with no explicit -o override above simply falls
# back to the default (unprefixed) template, i.e. it lands in Final files
# next to the video -- not ideal categorization, but never an error, and
# still correctly inside that video's own per-video folder.

# --- Post-processing: folder org, manifest, checksums, logs, Pure Video copy ---
# NOTE: %(filepath)q is used (not %(filepath)s). Newer yt-dlp versions refuse
# to expand plain %()s/%()l/etc. fields inside --exec commands (only d, i, f,
# q are allowed) because unquoted expansion of untrusted video metadata into
# a shell command is a code-injection risk. %q does its own quoting for the
# shell, so it is NOT wrapped in extra literal quotes here.
# -ExecutionPolicy Bypass (present in the Windows version) is dropped here:
# execution policies are a Windows-only concept and don't apply on Linux.
--exec "after_move:pwsh -NoProfile -File \"/home/linuxisthebest/yt-dlp/scripts/postprocess.ps1\" -FilePath %(filepath)q"
